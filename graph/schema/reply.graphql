type Reply { 
  id: ID!
  content: String
  create_at: String
  reply_id: ID
  is_uped: Boolean
  ups: [ID]!
  author: User 
}

type ReplyForMessage { 
  id: String
  content: String
  create_at: String
  ups: [ID]!
}

type ReplyRecent { 
  id: ID!
  title: String
  last_reply_at: String
  author: User 
}

