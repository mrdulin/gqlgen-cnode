type Reply { 
  id: ID!
  content: String
  createAt: String
  reply_id: ID
  isUped: Boolean
  ups: [ID]!
  author: User 
}

type ReplyForMessage { 
  id: String
  content: String
  createAt: String
  ups: [ID]!
}

type ReplyRecent { 
  id: ID!
  title: String
  lastReplyAt: String
  author: User 
}

